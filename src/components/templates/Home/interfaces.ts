import { type Props as ImageInterface } from '../../atoms/Image/interfaces';
import { type Props as NavbarInterface } from '../../organisms/Navbar/interfaces';

export interface Props {
  MyPictureProps?: ImageInterface;
  NavbarProps?: NavbarInterface;
}
